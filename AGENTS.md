# Agent Notes

## Core Commands
- **Dev server**: `npm run dev`
- **Build**: `npm run build`
- **Typecheck/Lint**: `npm run typecheck` (tsc) and `npm run lint`

## Settings & PRD
- Project settings live in `plans/settings.json` and mirror `ProjectSettingsSchema` (`src/lib/schemas.ts`)
- Product Requirements Document line in `plans/prd.json` 

## Expectations
- Keep documentation up to date
