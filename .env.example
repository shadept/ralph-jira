# OpenAI API Key (required for AI features)
OPENAI_API_KEY=sk-...

# Optional: customize AI model
# DEFAULT_AI_MODEL=gpt-4-turbo-preview

# Storage configuration
# Set to 'sqlite' to use SQLite database instead of filesystem
# RALPH_STORAGE_TYPE=sqlite

# SQLite database URL (optional, defaults to plans/ralph.db)
# DATABASE_URL="file:./plans/ralph.db"
